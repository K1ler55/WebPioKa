@model  WebApplication1.Models.flowdefmodel
@{ 
 Layout = null;
}


@using (Html.BeginForm("Pickthisflow","Pickflow",FormMethod.Post))
{

    @Html.DropDownListFor(model=>model.id_flowDefinition,new SelectList(Model.flowdefinitions, "id_flowDefinition"), "Select status",
    new { @class = "form-control" })
    <input type="submit" value="Submit"/>
}